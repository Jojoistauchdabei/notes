{
  "name": "grimoire",
  "version": "1.6.0",
  "private": true,
  "description": "Handschrift-Notizbuch im D&D-Papier-Stil mit GoodNotes-Import",
  "scripts": {
    "test": "node --test tests",
    "build": "rm -rf dist && mkdir -p dist && cp index.html manifest.webmanifest sw.js altes_Papier.png dist/ && cp -r css js icons screenshots dist/ && find dist -type f | sort"
  }
}
